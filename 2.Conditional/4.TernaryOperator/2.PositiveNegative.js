// ---------------------------------------------------------------------------------------------------------------------------------------------
//                                                                      23/08/24
// ---------------------------------------------------------------------------------------------------------------------------------------------

//w.a.p to check a number is +ve/-ve
var num = 90

console.log(num > 0 ? "POSITIVE NUMBER" : num < 0 ? "NEGATIVE NUMBER" : "ZERO");

var num = 0

console.log(num > 0 ? "POSITIVE NUMBER" : num < 0 ? "NEGATIVE NUMBER" : "ZERO");

var num = -90

console.log(num > 0 ? "POSITIVE NUMBER" : num < 0 ? "NEGATIVE NUMBER" : "ZERO");
